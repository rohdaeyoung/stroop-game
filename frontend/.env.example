# 이 파일을 .env 로 복사해서 쓰세요. (로컬 개발에서는 보통 필요 없습니다)
#
# 비워두면 Vite 프록시가 /api 를 http://localhost:8080 으로 넘깁니다.
# 배포(Render Static Site)에서는 백엔드 주소를 넣어야 합니다.
#
# VITE_API_BASE_URL=https://stroop-api.onrender.com/api
